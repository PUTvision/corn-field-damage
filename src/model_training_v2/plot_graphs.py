import random

from matplotlib import pyplot as plt


def plot_images_from_dataset(data_loader):
    dataset
    rng = random.Random(555)
    print(rng.random())
    rng.randrange(len())
    # image, mask = train_loader.dataset[222] # get some sample
    # plt.imshow(mask[0, :, :])
    # plt.show()
    # plt.imshow(mask[1, :, :])
    # plt.show()
    # plt.imshow(mask[2, :, :])
    # plt.show()
    # plt.imshow(image.transpose(1, 2, 0))



if __name__ == '__main__':
    plot_images_from_dataset('')
